# The canonical production address is https://www.4bros.website, set in lib/business.ts.
# NEXT_PUBLIC_SITE_URL is no longer used; an old Vercel value cannot override the domain.

# Your supplied Google verification token is already included in lib/business.ts.
# Optional override: paste ONLY the HTML tag's content value, then redeploy.
GOOGLE_SITE_VERIFICATION=
